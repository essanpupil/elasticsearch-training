---
- name: install elasticsearch initial node
  hosts: es-initial
  gather_facts: true
  become: true
  tasks:
    - name: Install Elasticsearch
      ansible.builtin.apt:
        name: "elasticsearch"
        state: present

    - name: Deploy Elasticsearch configuration
      ansible.builtin.template:
        src: cluster-elasticsearch.yml.j2
        dest: /etc/elasticsearch/elasticsearch.yml
        owner: root
        group: elasticsearch
        mode: '0660'

    - name: Ensure Elasticsearch service is enabled and started
      ansible.builtin.systemd:
        name: elasticsearch
        state: started
        enabled: true

    - name: Wait for 5 seconds for nodes to completely settle down
      ansible.builtin.pause:
        seconds: 5

    - name: Generate a fresh node enrollment token
      ansible.builtin.command: |
        /usr/share/elasticsearch/bin/elasticsearch-create-enrollment-token -s node --url https://{{ ansible_host }}:9200
      register: token_output
      changed_when: false

- name: Install elasticsearch join node
  hosts: es-join
  gather_facts: true
  become: true
  tasks:
    - name: Install Elasticsearch
      ansible.builtin.apt:
        name: "elasticsearch"
        state: present

    - name: Deploy Elasticsearch configuration
      ansible.builtin.template:
        src: cluster-elasticsearch.yml.j2
        dest: /etc/elasticsearch/elasticsearch.yml
        owner: root
        group: elasticsearch
        mode: '0660'

    - name: Run Elasticsearch reconfigure utility using enrollment token
      ansible.builtin.command: |
        /usr/share/elasticsearch/bin/elasticsearch-reconfigure-node --enrollment-token {{ hostvars['node1']['token_output']['stdout'] | trim }}
      # args:
      #   creates: /etc/elasticsearch/certs/transport.p12
      register: join_output

    - name: debug join 
      debug:
        msg: "{{ join_output }}"

    - name: Ensure Elasticsearch service is enabled and started
      ansible.builtin.systemd:
        name: elasticsearch
        state: started
        enabled: true

    - name: Wait for 10 seconds for nodes to completely settle down
      ansible.builtin.pause:
        seconds: 5

- name: Confirm elasticsearch cluster
  hosts: es-initial
  gather_facts: true
  become: true
  tasks:
    - name: Reset elastic user password
      when: inventory_hostname == 'node1'
      ansible.builtin.expect:
        command: "/usr/share/elasticsearch/bin/elasticsearch-reset-password --url https://{{ ansible_host }}:9200 -b -u elastic -i"
        responses:
          (?i)Enter password for \[elastic\]: "{{ elasticsearch_elastic_password }}"
          (?i)Re-enter password for \[elastic\]: "{{ elasticsearch_elastic_password }}"
      register: token_output
      changed_when: false

    - name: Query Elasticsearch API for active nodes
      when: inventory_hostname == 'node1'
      ansible.builtin.uri:
        url: "https://{{ ansible_host }}:9200/_cat/nodes?h=ip,name,role&format=json"
        method: GET
        user: elastic
        password: "{{ elasticsearch_elastic_password }}"
        force_basic_auth: true
        validate_certs: false
      register: cluster_status

    - name: Print the Cluster Status and Active Node List
      when: inventory_hostname == 'node1'
      ansible.builtin.debug:
        msg: "{{ cluster_status.json }}"

    # - name: Get current timestamp
    #   setup:
    #     filter: ansible_date_time

    # - name: Backup existing certificates by renaming them
    #   ansible.builtin.shell: |
    #     if [ -f "{{ item }}" ]; then
    #       mv "{{ item }}" "{{ item }}_{{ ansible_date_time.iso8601_basic_short }}"
    #     fi
    #   loop:
    #     - /usr/share/elasticsearch/elastic-stack-ca.p12
    #     - /usr/share/elasticsearch/elastic-certificates.p12
    #   register: rename_result
    #   changed_when: "rename_result.rc == 0"

    # - name: Generate Certificate Authority
    #   when: inventory_hostname == 'node1'
    #   ansible.builtin.command:
    #     cmd: "{{ es_bin_dir }}/elasticsearch-certutil ca --pass '' --out elastic-stack-ca.p12 --silent"
    #   changed_when: true

    # - name: Generate TLS Certificate
    #   when: inventory_hostname == 'node1'
    #   ansible.builtin.command:
    #     cmd: "{{ es_bin_dir }}/elasticsearch-certutil cert --ca elastic-stack-ca.p12 --pass '' --ca-pass '' --out elastic-certificates.p12 --silent"
    #   changed_when: true

    # - name: Fetch CA certificate
    #   when: inventory_hostname == 'node1'
    #   fetch:
    #     src: "/usr/share/elasticsearch/elastic-stack-ca.p12"
    #     dest: /tmp/elastic-stack-ca.p12
    #     flat: true

    # - name: Fetch node certificate
    #   when: inventory_hostname == 'node1'
    #   fetch:
    #     src: "/usr/share/elasticsearch/elastic-certificates.p12"
    #     dest: /tmp/elastic-certificates.p12
    #     flat: true

    # - name: Copy CA certificate to all elasticsearch nodes
    #   ansible.builtin.copy:
    #     src: /tmp/elastic-stack-ca.p12
    #     dest: /etc/elasticsearch/certs/elastic-stack-ca.p12
    #     owner: root
    #     group: elasticsearch
    #     mode: '0660'

    # - name: Copy node certificate to all elasticsearch nodes
    #   ansible.builtin.copy:
    #     src: /tmp/elastic-certificates.p12
    #     dest: /etc/elasticsearch/certs/elastic-certificates.p12
    #     owner: root
    #     group: elasticsearch
    #     mode: '0660'
